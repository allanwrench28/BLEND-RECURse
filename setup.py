from setuptools import setup, find_packages

setup(
    name='blend-recurse',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[],
    author='allanwrench28',
    description='GHST MoE Ghost Teams and UIUX framework',
    include_package_data=True,
    python_requires='>=3.7',
)
